{
  "name": "farm-registry-admin-functions",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions"
  },
  "dependencies": {
    "firebase-admin": "^13.0.2",
    "firebase-functions": "^6.4.0"
  },
  "devDependencies": {
    "typescript": "~5.8.3"
  }
}
